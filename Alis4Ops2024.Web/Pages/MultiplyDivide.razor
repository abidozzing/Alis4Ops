@page "/multiplydivide"
@using Alis4Ops2024.Web.Core
@using Alis4Ops2024.Web.Models
@using Alis4Ops2024.Web.Layout
@using Microsoft.JSInterop
@inject IJSRuntime JSRuntime
@inject NavigationManager _navigationManager
@using System
@inject Blazored.SessionStorage.ISessionStorageService sessionStorage
<link href="css/site.css" rel="stylesheet" />

<PageTitle>Add</PageTitle>

<div class="container col-12">

    @if (Num <= 0 || Num > 2)
    {
        <div>
            <Multiply AnsweredCorrectly="HandleGenerateQuestion" SelectedItem="Multiply-Missing" LowerRange ="1" UpperRange ="12" />
        </div>
    }
    else
    {
        <div>
            <Divide AnsweredCorrectly="HandleGenerateQuestion" SelectedItem="Divide-Missing" LowerRange="1" UpperRange="12" />
        </div>
    }

</div>

@code {

    private Random random = new Random();

    private int Num { get; set; } = 0;

    [Parameter]
    public EventCallback AnsweredCorrectly { get; set; }

    protected override void OnInitialized()
    {
        HandleGenerateQuestion();
    }

    private void HandleGenerateQuestion()
    {
        Num = random.Next(0, 2);

    }

}






